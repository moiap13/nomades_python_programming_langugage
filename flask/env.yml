name: nomades_flask_312
channels:
  - defaults
  - conda-forge
dependencies:
  # base python stuff ======================================================
  - python=3.12
  - pip
  - flask
  - WTForms
  - email_validator
  - ipykernel
  # extra from pip =========================================================
  - pip:
    - firebase-admin